spring:
  application:
    name: uploader-service

server:
  port: 9050
  servlet:
    context-path: /api/v1/uploads

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

cloudinary:
  api-key: YOUR_API_KEY
  cloud-name: YOUR_CLOUD_NAME
  api-secret: YOUR_API_SECRET